"""
smc/ — the SMC/ICT structural core (fork: options_trader_smc). v1.0
v1.0 — 2026-08-18 — INITIAL. primitives.py (pure functions) +
        smc_engine.py (stateful composer + anticipatory setup lifecycle).
        See docs/SMC_TRUTHS.md for the definitional grammar and
        docs/BACKLOG.md for what is deliberately not built yet.
"""
